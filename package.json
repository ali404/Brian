{
  "name": "brian",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run client\"",
    "server": "cd server && node server.js",
    "client": "cd client && npm start"
  },
  "dependencies": {
    "concurrently": "^3.4.0"
  }
}
